module Step.DataTable

open UniSpec


[<Given>]
let ``a simple data table`` (t: Table) = printfn $"测试到了"

[<Given>]
let ``a data table with only one line`` () = failwith "Need design"

[<Given>]
let ``a data table with different columns`` () = failwith "Need design"

[<Given>]
let ``a data table with comments and newlines inside`` () = failwith "Need design"
